overwrite: true
schema: "http://127.0.0.1:4000/graphql"
documents: null
debug: true
generates:
  ./codegen-outputs/graphql.ts:
    plugins:
      - "typescript"
  ./codegen-outputs/graphql.schema.json:
    plugins:
      - "introspection"
