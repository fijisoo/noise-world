/**
 * github-manifesto-changelog controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::github-manifesto-changelog.github-manifesto-changelog');
