overwrite: true
schema: "https://noise-world-production.up.railway.app/graphql"
documents: null
debug: true
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
